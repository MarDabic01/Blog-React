import { HStack } from "@chakra-ui/layout";

const Header = () => {
  return (
    <HStack>
      <HStack w="50%"></HStack>
      <HStack w="50%"></HStack>
    </HStack>
  );
};

export default Header;
